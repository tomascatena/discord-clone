import { styled } from '@mui/system';

export const WelcomeMessageContainer = styled('div')(() => ({
  flexGrow: 1,
  height: '100%',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
}));
