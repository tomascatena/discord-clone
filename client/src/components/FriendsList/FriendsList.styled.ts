import { styled } from '@mui/system';

export const FriendsListContainer = styled('div')(() => ({
  flex: 1,
  width: '100%',
  padding: '0.5rem 0.8rem',
  overflow: 'hidden',
  overflowY: 'scroll',
  marginBottom: '1rem',
}));
